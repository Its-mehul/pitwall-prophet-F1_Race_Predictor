Add more neural network/training metrics to represent the underlying model in the ui/presentation.

Maybe add an animation that shows how the data passes through the neural network.

Use more rank appropriate accuracy/loss metrics instead of mean absolute error.

Shift invariant